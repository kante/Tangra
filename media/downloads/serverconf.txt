CCM Server:localhost
;DO NOT change the line below unless using a pool of SimServers. - The default setting is no.
;the lines should follow the following format exactly
;use pool of SimServers=no		;if using a local SimServer as a separate process
;use pool of SimServers=yes		;if using a pool
use pool of SimServers=no
;DO NOT change the line below unless using a SimServer within the pvm.
; - The default setting is yes.
;the lines should follow the following format exactly
;separate SimServer=no		;if the pvm instantiates the SimServer, and not the batch file
;separate SimServer=yes		;if using a local SimServer as a separate process
separate SimServer=no
